export interface TaskItem {
    name: string;
    due_date: string;
    details: string;
    status: string;
    
}

export interface List {
    id: string;
    name: string;
    description: string;
}

